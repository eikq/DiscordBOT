# Jarvis Memory + UI Addendum

Copy these files into the root of the existing Digital Me repository.

Do not overwrite current `PROJECT_CONTEXT.md`, `AGENTS.md`, or the main handoff pack.

After copying, open Cursor and use `CURSOR_MEMORY_UI_PROMPT.md`.

These are design instructions and future tasks, not proof that memory migration or the 3D UI is implemented.
